<project name="ABC-Framework" default="build" basedir=".">
    <!-- Run composer update and executes various other updates -->
    <target name="update">
        <exec command="composer update" checkreturn="true" passthru="true"/>
    </target>

    <!-- Runs ApiGen -->
    <target name="apigen">
        <exec command="apigen generate --todo --tree --source src --destination doc" passthru="true"
              checkreturn="true"/>
    </target>

    <!-- Runs all unit tests -->
    <target name="unit">
        <move file="test/C.txt" tofile="test/C.php" haltonerror="false"/>
        <exec command="bin/phpunit" passthru="true" checkreturn="true"/>
        <move file="test/C.php" tofile="test/C.txt" haltonerror="true"/>
    </target>
    <target name="build"/>
</project>
